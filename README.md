# chatter-sample-server

This is an ExpressJS based server for a real-time chat.

To run the server, install first the dependencies:

``` $ npm i ```

Then run the server:

``` $ npm start ```

This will run the server.